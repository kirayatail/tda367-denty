package com.googlecode.tda367.denty.constants;

/**
 * All possible values that can be transmitted between two bodies when they
 * collide. Each body is responsible for executing any events flagged by these
 * markers.
 * 
 * @author max
 * 
 */
public enum Hit {
	DENTY, ENEMY, NORMAL_DMG, PIERCING_DMG, DMG_HALF, KEY, LOCK
}
