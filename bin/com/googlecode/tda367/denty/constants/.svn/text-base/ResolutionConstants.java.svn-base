package com.googlecode.tda367.denty.constants;

/**
 * Screen resolution passes through here between game initiation and the view
 * 
 * @author max
 * 
 */
public class ResolutionConstants {
	// inner screen resolution through tile size
	// cannot be final since they differ on every system.
	public static float WIDTH = 0;
	public static float HEIGHT = 0;
}
